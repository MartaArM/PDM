<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Ver_horas_usuario"></activity>
        <activity android:name=".Editar_usuario" />
        <activity android:name=".Ver_usuario" />
        <activity android:name=".Ver_usuarios" />
        <activity android:name=".Agregar_usuario" />
        <activity android:name=".Administracion" />
        <activity android:name=".Opciones" />
        <activity android:name=".motivo_tarde" />
        <activity android:name=".lugar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>